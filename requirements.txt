# Core ML & data
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3
matplotlib>=3.7
seaborn>=0.12

# UART live streaming (optional – only needed for --uart mode)
pyserial>=3.5

# TinyML export (optional)
# emlearn>=0.17          # pip install emlearn
# tensorflow>=2.13       # pip install tensorflow  (for TFLite export)
